<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sms.mapper.auth.LoginEmployeeMapper">

    <select id="selectActiveEmployeesByEmpId" resultType="com.example.sms.vo.auth.LoginEmployeeVO">
        SELECT
            E.EMP_ID AS empId,
            E.DEP_ID AS depId,
            E.EMP_NM AS empNm,
            E.ACT_YN AS actYn,
            D.DEP_NM AS depNm,
            D.ACT_YN AS depActYn
        FROM SMS.EMP E
        INNER JOIN SMS.DEP D
            ON D.DEP_ID = E.DEP_ID
        WHERE E.EMP_ID = #{empId}
          AND E.ACT_YN = 'Y'
          AND D.ACT_YN = 'Y'
        ORDER BY E.DEP_ID
    </select>

</mapper>
